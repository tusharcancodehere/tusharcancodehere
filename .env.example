# Optional: a GitHub Personal Access Token (classic) with read:user scope.
# Prevents API rate-limiting when running the update script frequently.
# Never commit your actual token.
#
# To create one: https://github.com/settings/tokens
GITHUB_TOKEN=
